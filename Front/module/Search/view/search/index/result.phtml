<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Google Search Results</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .search-bar {
            background-color: #f2f2f2;
            padding: 10px;
            text-align: center;
        }
        .search-input {
            width: 40%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .search-button {
            padding: 8px 15px;
            background-color: #4285f4;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .search-button:hover {
            background-color: #357ae8;
        }

        .settings-button {
            padding: 8px 15px;
            background-color: #696969;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .settings-button:hover {
            background-color: #a9a9a9;
        }
        .search-result {
            margin: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .result-title {
            font-weight: bold;
            color: #1a0dab;
        }
        .result-url {
            color: #006621;
        }
        .result-description {
            color: #545454;
            word-break: break-word;
        }
    </style>
</head>
<body>
    <div class="search-bar">
        <button class="settings-button">Ajustes</button>
        <input type="text" class="search-input" placeholder="Buscar...">
        <button class="search-button">Buscar</button>
    </div>
    <?php foreach ($data['hits']['hits'] as $result): ?>
        <div class="search-result">
            <a href="#" class="result-title"><?= $result['_source']['title'] ?></a>
            <div class="result-url"><?= $result['_source']['path'] ?></div>
            <?php
                $description = $result['_source']['body'];
                $truncatedDescription = strlen($description) > 600 ? substr($description, 0, 600) . '...' : $description;
            ?>
            
            <div class="result-description"><?= $truncatedDescription ?></div>
        </div>
    <?php endforeach; ?>
    <!-- Add more search results as needed -->
    <script>
    const searchInput = document.getElementById('search-input');
    const searchButton = document.getElementById('search-button');

    searchButton.addEventListener('click', () => {

        window.location.href="/search/results?q="+searchInput.value;
    });

  </script>
</body>
</html>
